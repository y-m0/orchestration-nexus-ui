
import { useState } from "react";

export function WorkflowCanvas() {
  // This would be replaced with actual graph/workflow visualization logic
  return (
    <div className="flex-1 bg-gray-900 flex items-center justify-center p-6 overflow-auto">
      <div className="text-center text-muted-foreground">
        <p className="mb-2">Drag components from the sidebar to start building your workflow</p>
        <p className="text-sm">Click to connect nodes and create a workflow</p>
      </div>
    </div>
  );
}
